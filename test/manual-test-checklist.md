# デジタルタイムシート 仮想スクロール実装テスト

## テスト日時
2026-01-19 11:35

## テスト環境
- ブラウザ: Chrome/Edge
- 実装: 仮想スクロール対応版
- 行数: 144行（12秒 × 12fps）

---

## 1. 基本操作テスト

### 1.1 セル入力
- [ ] A1に「1」を入力 -> 正常に入力される
- [ ] A2に「2」を入力 -> 正常に入力される
- [ ] A2を選択してEnter -> A3に移動、A2は「-」表示
- [ ] A3を選択してEnter -> A4に移動、A3は「-」表示

### 1.2 矢印キー移動
- [ ] A1選択時に(down) -> A2に移動
- [ ] A2選択時に-> -> B2に移動
- [ ] B2選択時に(up) -> B1に移動
- [ ] B1選択時に(left) -> A1に移動

### 1.3 +/-キー
- [ ] A1に「1」、A2選択して+ -> A2に「2」が入力され、A3に移動
- [ ] A3に「5」、A4選択して- -> A4に「4」が入力され、A5に移動

### 1.4 複数選択
- [ ] A1-A3をドラッグ選択 -> 3セル選択される
- [ ] 「1」入力してEnter -> A1に「1」、A2-A3に「-」、選択がA4-A6に移動

---

## 2. 特殊表示テスト

### 2.1 ×印（先頭セル、下に数字）
- [ ] 表を初期化
- [ ] A5に「1」を入力
- [ ] A1に×印が表示される

### 2.2 波線（×印の下）
- [ ] 上記の続き
- [ ] A2-A4に波線が表示される
- [ ] 波線が途切れず連続している

### 2.3 縦線（尺の最後まで）
- [ ] 表を初期化
- [ ] A1に「1」を入力
- [ ] A2を選択して.キーを押す
- [ ] A2から最後（A144）まで縦線が表示される
- [ ] 縦線が途切れず連続している

### 2.4 縦線の開始・継続・終了
- [ ] A2: 縦線の開始（上半分のみ）
- [ ] A3-A143: 縦線の継続（全体）
- [ ] A144: 縦線の終了（下半分のみ）

---

## 3. 編集操作テスト

### 3.1 Delete キー
- [ ] A1に「1」、A2-A5に「-」の状態
- [ ] A3をDeleteで削除 -> A3が空、A4に「1」が表示、A5から縦線

### 3.2 0入力（空セル化）
- [ ] A1に「1」を入力
- [ ] A1に「0」を入力 -> A1が空セルになる

### 3.3 複数セル削除
- [ ] A1-A3に「1」「-」「-」
- [ ] A2-A3を選択してDelete -> 両方空セルになる

---

## 4. Undo/Redo テスト

### 4.1 Undo
- [ ] A1に「1」を入力
- [ ] Ctrl+Z -> A1が空に戻る

### 4.2 Redo
- [ ] 上記の続き
- [ ] Ctrl+Y -> A1に「1」が再入力される

### 4.3 複数Undo
- [ ] A1に「1」、A2に「2」、A3に「3」を入力
- [ ] Ctrl+Zを3回 -> すべて空に戻る

---

## 5. 行列操作テスト

### 5.1 行挿入（先頭）
- [ ] A1に「1」を入力
- [ ] 左ヘッダー右クリック -> 「先頭に行挿入」
- [ ] 行が挿入され、元のデータが下にずれる
- [ ] 挿入行が赤背景で表示される

### 5.2 行挿入（途中）
- [ ] 5行目を右クリック -> 「途中に行挿入」
- [ ] 5行目に挿入され、以降が下にずれる

### 5.3 行削除
- [ ] 挿入した行を右クリック -> 「選択している行を削除」
- [ ] 行が削除され、以降が上に詰まる

### 5.4 Undo/Redo（行操作）
- [ ] 行挿入後にCtrl+Z -> 挿入が取り消される
- [ ] Ctrl+Y -> 挿入が復元される

---

## 6. シート操作テスト

### 6.1 新規シート作成
- [ ] 「新規」ボタン -> 名前入力 -> 新しいシートが作成される
- [ ] タブが追加される

### 6.2 シート切り替え
- [ ] シート1で入力後、シート2に切り替え -> 表示が正しく切り替わる
- [ ] シート1に戻る -> 入力が保持されている

### 6.3 表初期化
- [ ] データ入力後、「表初期化」 -> すべてのデータが消える
- [ ] 挿入行の色表示も消える

---

## 7. ファイル操作テスト

### 7.1 保存
- [ ] データ入力後、「保存」 -> JSONファイルがダウンロードされる
- [ ] ファイル名がシート名になっている

### 7.2 読み込み
- [ ] 保存したファイルを「開く」 -> データが正しく復元される
- [ ] 特殊表示も正しく復元される

### 7.3 JSX出力
- [ ] データ入力後、「JSXスクリプトとして出力」
- [ ] setValuesAtTimesが使用されている

---

## 8. パフォーマンステスト

### 8.1 DOM要素数確認
コンソールで実行：
```javascript
document.querySelectorAll('td').length
```
- [ ] 結果: 3888個（144行×27列）
- [ ] 期待: 全行表示されている（144行なので仮想スクロールは無効）

### 8.2 スクロール動作
- [ ] 表を上下にスクロール -> スムーズに動作
- [ ] レンダリングの遅延がない

### 8.3 大量入力
- [ ] 列全選択して「1」入力 -> 一瞬で入力完了
- [ ] 表示が正しく更新される

---

## 9. Shift選択テスト

### 9.1 Shift+矢印
- [ ] A1選択 -> Shift+(down) -> A1-A2選択
- [ ] さらにShift+(down) -> A1-A3選択
- [ ] Shift+-> -> A1-A3, B1-B3選択（2×3）

### 9.2 Shift+Home/End
- [ ] A50選択 -> Shift+Home -> A1-A50選択
- [ ] A50選択 -> Shift+End -> A50-A144選択

---

## 10. 尺変更テスト

### 10.1 尺変更
- [ ] 「尺変更」ボタン -> 「6+0」入力
- [ ] 行数が72行（6秒×12fps）に変更される

### 10.2 尺変更後の動作
- [ ] 入力が正常に動作する
- [ ] 特殊表示が正しく動作する

---

## テスト結果

### 成功したテスト
- 

### 失敗したテスト
- 

### 備考
- 
